let blockTypes = {
    "join": {
        "title": "join",
        "type": "join",
        "description": "join two dataframes",
        "color": "red",
        "icon": "call_merge"
    }
}
export default blockTypes