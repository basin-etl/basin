{
    "title": "aggregate",
    "type": "aggregate",
    "description": "groupby",
    "color": "red",
    "icon": "call_merge",
    "inputs": [
        {"id":"df","label":"df", "type": "dataframe"}
    ],
    "outputs": [
        {"id":"df","label":"df", "type": "dataframe"}
    ],
    "comment_template": "aggregate by ${columnNames(props.groupBy)}"
}